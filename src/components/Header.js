import React from "react";

const Header = () => {
  return (
    <header className="header">
      <h1>Fixyo</h1>
      <nav>
        <a href="#about">About</a>
        <a href="#join">Join</a>
      </nav>
    </header>
  );
};

export default Header;
